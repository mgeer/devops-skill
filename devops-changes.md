# DevOps Skill 开发 — Change 划分

## 划分原则

**底层设计原则：** 所有设计决策以"AI 操作不易出错"为第一判断标准。文件结构为 AI 优化，人需要的视图由 AI 按需生成。

**纵向切片：** 每个 change 同时包含该领域的架构规范（Phase 1）和场景设计（Phase 2），而非先做完所有规范再做场景。规范和场景互相验证，问题当场解决。

**每个 change 的标准交付物：**
- 该领域的架构规范章节（写入 platform-spec.md）
- 该领域的场景交互设计（写入 flows/*.md）
- 相关待决策项的最终决定
- 验证场景 + 明确的验收条件
- 与其他 change 的接口约定

---

## 依赖关系

```
Change 1 (平台基础规范 — 厚地基)
    |
    +---> Change 2 (服务生命周期与依赖管理)
    |         |
    |         +---> Change 3 (CI/CD 与部署流程)
    |
    +---> Change 4 (可观测性)
    |
    +---> Change 5 (网络安全与备份)

Change 2~5 完成后
    |
    +---> Change 6 (高级特性)

所有上面完成后
    |
    +---> Change 7 (端到端验收与打磨)
```

---

## Change 1：平台基础规范

**定位：** 厚地基。不只是目录结构，而是整个平台的运行规则。后续所有 change 都在这个框架内工作。

**Phase 1 模块：**
- 1.1 整体架构（三层分离、配置仓库结构、职责边界）
- 1.3 资源管理规则（所有权模型、依赖引用机制、资源生命周期）
- 1.7 配置文件 Schema（.devops.yaml + platform-inventory.yaml 最终字段定义）
- 1.8 命名规范（服务名、资源名、namespace、label 的规则）
- 1.11 环境差异化（overlay 方案、环境变量管理）
- GitOps 工作流联动（CI 怎么更新配置仓库？token 还是 deploy key？）
- ArgoCD 组织方式（App-of-Apps？ApplicationSet？同步策略）

**需要做出的决策：**
- #1 配置管理方式：Kustomize vs Helm
- #3 环境差异化方案
- #5 Skill 分发方式（基础架构决策，提前确定）
- #12 集群拓扑：单集群 vs 多集群
- #13 配置仓库拆分：全公司一个 vs 按域拆分
- #14 .devops.yaml vs dependencies.yaml 如何去重

**Phase 2 交付物：**
- skill.md 骨架（核心原则 + AI 处理逻辑，不含具体 flow）

**验证方式：**
- 画出完整的配置仓库目录结构，确认无遗漏
- 写出 .devops.yaml 和 platform-inventory.yaml 的完整示例，用真实场景填充
- 确认命名规范覆盖所有资源类型
- 画出"CI 更新配置仓库 → ArgoCD 同步"的完整链路，确认技术可行

**验收条件（必须能回答）：**
- 一个新服务在配置仓库中的完整目录结构是什么？
- 两个仓库之间的自动联动机制是什么？凭证怎么管理？
- 同一个资源在不同环境的差异怎么表达？
- 命名冲突怎么防止？

**与后续 change 的接口约定：**
- Change 2 使用本 change 定义的目录结构和命名规范
- Change 3 使用本 change 定义的 GitOps 联动机制
- Change 4 使用本 change 定义的 namespace 和 label 规范

---

## Change 2：服务生命周期与依赖管理

**定位：** 开发者最核心的日常操作。聚焦"创建服务、管理依赖、下线服务"的交互设计。

**Phase 1 模块：**
- 1.10 Secret 管理（Operator 密码传递、业务 Secret — 创建服务时就需要）
- 1.12 资源配额（CPU/内存规格等级 — 创建服务时就要选择）

**需要做出的决策：**
- #2 Secret 管理方案
- #4 Skill 操作配置仓库方式（创建服务 flow 就需要操作配置仓库）
- #8 技术栈支持范围
- #16 资源规格等级

**Phase 2 交付物：**
- flows/create-service.md（新建服务完整流程）
- flows/add-dependency.md（创建/引用中间件）
- flows/decommission.md（服务下线与资源清理）

**验证场景：**
- 场景 A：新建 Go 服务，需要 MySQL + Kafka（producer）
- 场景 B：另一个服务消费上面的 Kafka topic（consumer）
- 场景 C：服务 A 下线，但 Kafka topic 还有消费者
- 场景 D：新建服务时，需要的 MySQL Operator 尚未部署
- 场景 E：服务需要第三方 API Key，怎么安全存储和注入？
- 场景 F：新建服务时选择资源规格，超出默认配额怎么处理？

**验收条件（必须能回答）：**
- 从"对 AI 说我要建一个服务"到"服务配置全部就绪"，完整交互流程是什么？每一步 AI 问什么、用户答什么、AI 做什么？
- Kafka topic 被两个服务引用，在配置仓库中的表现是什么？能自动生成依赖图吗？
- 删除一个被引用的资源时，AI 的行为是什么？
- Secret 从生成到注入 Pod 的完整链路？

**与后续 change 的接口约定：**
- Change 3 接手"服务创建完成后，代码推送触发 CI/CD"的流程
- 本 change 约定：创建服务时自动生成 .gitlab-ci.yml 骨架，具体 CI 阶段由 Change 3 定义
- 本 change 约定：创建服务时自动生成 service-monitor.yaml，具体监控规则由 Change 4 定义

---

## Change 3：CI/CD 与部署流程

**定位：** 代码提交到上线的完整链路。数据库变更和回滚是难点。

**Phase 1 模块：**
- 1.4 CI/CD 流程（流水线阶段、镜像 tag 策略、环境推进策略、数据库变更管理）
- 1.5 回滚策略（纯代码回滚、涉及 DB 变更的回滚、多服务联动回滚）

**需要做出的决策：**
- #9 数据库变更工具

**Phase 2 交付物：**
- flows/deploy.md（部署与环境推进）
- flows/db-migration.md（数据库变更管理）

**验证场景：**
- 场景 A：纯代码变更，从 push 到 prod 上线完整流程
- 场景 B：带 DB migration 的变更，部署顺序怎么处理？
- 场景 C：上线后发现问题，纯代码回滚
- 场景 D：上线后发现问题，涉及 DB 变更的回滚
- 场景 E：Pipeline 失败（测试不过、构建失败、安全扫描不过），AI 怎么引导？

**验收条件（必须能回答）：**
- .gitlab-ci.yml 的标准阶段有哪些？每个阶段做什么？
- 镜像 tag 策略是什么？怎么保证回滚时能找到对应镜像？
- 从 dev 推进到 staging 再到 prod 的具体操作是什么？
- 带 DB 变更的发布，部署和 migration 的顺序是什么？回滚时怎么办？
- Pipeline 失败时，AI 能做什么？不能做什么？

**与后续 change 的接口约定：**
- Change 4 在 CI 中加入自动检查（比如 /metrics 端点是否存在）
- 回滚操作可能需要 Change 4 的监控数据来判断是否需要回滚

---

## Change 4：可观测性

**定位：** 让服务"可见"，让开发者能自助排查和配告警，不依赖运维。

**Phase 1 模块：**
- 1.6 可观测性（分层标准、自动 vs 手动、日志格式、指标命名规范、告警分级）

**需要做出的决策：**
- #7 日志格式

**Phase 2 交付物：**
- flows/observe-alert.md（可观测性与告警配置）
- flows/troubleshoot.md（线上排查与回滚）

**验证场景：**
- 场景 A：新服务接入后，不做任何配置，自动获得了哪些监控？在 Grafana 怎么看？
- 场景 B："帮我加个告警，订单成功率低于 95% 就报警" — 自然语言转 PromQL，AI 怎么确认准确性？
- 场景 C："线上 5xx 增多，帮我排查" — AI 从哪些数据源聚合信息？按什么顺序展示？
- 场景 D：开发者想加业务埋点（如"订单金额分布"），代码和配置分别要做什么？

**验收条件（必须能回答）：**
- 可观测性的三层（自动/埋点/链路追踪）各自包含什么？开发者需要做什么？
- 告警规则从自然语言到生效的完整链路？中间有几次确认？
- AI 排查问题时，数据来源优先级是什么？（指标 → 日志 → 链路？）
- 开发者在不找运维的情况下，能自助完成哪些操作？

**与后续 change 的接口约定：**
- 本 change 约定 service-monitor.yaml 的标准格式，Change 2 在创建服务时自动生成
- 排查流程中的回滚操作，使用 Change 3 定义的回滚机制

---

## Change 5：网络安全与备份

**定位：** 生产加固。确保服务在生产环境安全、可靠地运行。

**Phase 1 模块：**
- 1.9 网络与安全（NetworkPolicy、中间件访问控制）
- 1.13 备份恢复（数据库自动备份策略、保留周期、恢复方式）

**需要做出的决策：**
- #6 服务间调用方式
- #15 数据库备份方案

**Phase 2 交付物：**
- 无独立 flow，更新已有 flow：
  - create-service.md 增加 NetworkPolicy 自动生成
  - add-dependency.md 增加中间件访问控制配置

**验证场景：**
- 场景 A：服务 A 不应该访问服务 B 的数据库，NetworkPolicy 怎么自动生成？
- 场景 B：数据库误删数据，怎么恢复？开发者能自助还是需要运维？
- 场景 C：服务 A 需要调用服务 B 的 API，网络层面需要做什么？

**验收条件（必须能回答）：**
- 创建服务时自动生成哪些安全配置？
- 数据库备份的频率、保留周期、存储位置？
- 恢复操作的完整流程？权限要求？

**与后续 change 的接口约定：**
- 本 change 定义的 NetworkPolicy 模板，Change 2 的 flow 在创建服务时自动应用

---

## Change 6：高级特性

**定位：** 锦上添花。核心流程跑通后，按实际痛点决定优先级。每个特性可以独立讨论是否需要、MVP 方案是什么。

**Phase 1 模块：**
- 1.2 多集群设计
- 1.14 灰度发布
- 1.15 本地开发

**需要做出的决策：**
- #10 灰度发布方案
- #11 本地开发方案

**Phase 2 交付物：**
- flows/local-dev.md（本地开发环境配置）

**讨论重点（先决定"是否需要"再讨论"怎么做"）：**
- 多集群：现阶段是否需要？先单集群 + namespace 隔离够不够用？什么时候必须上多集群？
- 灰度发布：先用原生滚动更新，什么场景下必须上金丝雀？
- 本地开发：docker-compose 够用还是需要 telepresence？

**验收条件（必须能回答）：**
- 每个特性的"触发条件"是什么？什么时候从"不需要"变成"必须有"？
- MVP 方案和完整方案分别是什么？

---

## Change 7：端到端验收与打磨

**定位：** 不是新功能开发，而是把前面所有成果串起来验收、发现缝隙、打磨体验。

**内容：**
- 用一个真实项目（或高仿真模拟）从头到尾走一遍完整流程
- 发现各 change 之间的衔接问题并修复
- 打磨 skill.md 的意图识别和场景分发逻辑
- 补充 examples/（从验收过程中产生的真实配置）
- 确认所有 flow 的交互体验符合 P0-P3 原则

**端到端验证流程：**
```
1. 新建服务（Change 2）
   "帮我创建一个 order-service，Go 语言，需要 MySQL 和 Kafka"
       |
2. 本地开发（Change 6）
   "怎么在本地跑起来？"
       |
3. 提交代码（Change 3）
   push 代码 → CI 运行 → 镜像构建 → 部署到 dev
       |
4. 加监控（Change 4）
   "帮我加个告警，接口 P99 超过 500ms 就报警"
       |
5. 推进到 prod（Change 3）
   "部署到生产环境"
       |
6. 模拟故障（Change 4）
   "线上 5xx 增多，帮我排查"
       |
7. 回滚（Change 3）
   "帮我回滚到上个版本"
       |
8. 加新依赖（Change 2）
   "需要加一个 Redis 缓存"
       |
9. 另一个服务消费 Kafka（Change 2）
   切换到 notification-service
   "我要消费 order-events 这个 topic"
```

**验收条件（必须能回答）：**
- 整个流程中有几次"卡住了不知道该怎么指挥 AI"的情况？
- 有几次 AI 的操作需要用户纠正？
- 各 change 之间的衔接是否顺畅？有没有信息断层？
- P0-P3 原则是否都得到了遵守？

---

## 建议执行顺序

```
顺序    Change                  预估讨论轮次    前置依赖
----    ------                  -----------    --------
 1      平台基础规范              2-3 轮        无
 2      服务生命周期与依赖管理     2-3 轮        Change 1
 3      CI/CD 与部署流程          2-3 轮        Change 2
 4      可观测性                  1-2 轮        Change 1
 5      网络安全与备份             1-2 轮        Change 1
 6      高级特性                  1-2 轮        Change 2~5
 7      端到端验收与打磨           1-2 轮        全部

Change 3/4/5 之间无强依赖，可按优先级调整顺序。
建议 Change 3 在 Change 2 之后（部署依赖服务创建）。
Change 4 和 5 可并行或按需调整。
```
