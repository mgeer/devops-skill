apiVersion: v1
kind: Service
metadata:
  name: order-service
  labels:
    app: order-service
    domain: trade
    managed-by: devops-skill
spec:
  selector:
    app: order-service
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
